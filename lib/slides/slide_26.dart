import 'package:flutter/material.dart';

/// Slide 26 — Seção 08: Comparativo Final
class Slide26 extends StatelessWidget {
  const Slide26({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          RichText(
            textAlign: TextAlign.center,
            text: const TextSpan(
              style: TextStyle(fontSize: 42, fontWeight: FontWeight.w700),
              children: [
                TextSpan(text: '08', style: TextStyle(color: Color(0xFFFFA657))),
                TextSpan(text: ' — Comparativo Final', style: TextStyle(color: Color(0xFFF0F0F0))),
              ],
            ),
          ),
          const SizedBox(height: 16),
          const Text(
            'Polling vs Interrupt vs DMA',
            textAlign: TextAlign.center,
            style: TextStyle(fontSize: 18, color: Color(0xFF8B949E)),
          ),
        ],
      ),
    );
  }
}
